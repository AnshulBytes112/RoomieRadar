# Environment Variables Template for Production
# Copy this file to .env and fill in your actual values
# DO NOT commit .env file to version control!

# ===== REQUIRED VARIABLES =====

# Active Spring Profile (dev or prod)
SPRING_PROFILES_ACTIVE=prod

# Database Configuration
DB_URL=jdbc:postgresql://your-db-host:5432/roomie_radar
DB_USERNAME=your_database_username
DB_PASSWORD=your_database_password

# JWT Secret (minimum 32 characters - generate with: openssl rand -base64 32)
JWT_SECRET=your_secure_jwt_secret_key_here

# Email Configuration (for notifications)
MAIL_USERNAME=your_email@gmail.com
MAIL_PASSWORD=your_app_specific_password

# ===== OPTIONAL VARIABLES =====

# Server Port (default: 8080)
PORT=8080

# CORS Allowed Origins (comma-separated, no spaces)
ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# File Upload Directory (default: /app/uploads)
FILE_UPLOAD_DIR=/app/uploads

# Email Server Configuration (defaults shown)
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587

# ===== DEVELOPMENT EXAMPLE =====
# For local development, use these values:
# SPRING_PROFILES_ACTIVE=dev
# (All other variables will use defaults from application-dev.properties)
