# Development Profile Configuration
# Activate this profile with: -Dspring.profiles.active=dev or environment variable SPRING_PROFILES_ACTIVE=dev

# Database Configuration - Local PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/roomie_radar
spring.datasource.username=postgres
spring.datasource.password=12345
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Settings - Development
# Using 'update' to automatically update database schema during development
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# JWT Configuration - Development Secret
jwt.secret=TaK+HaV^uvCHEFsE94an30sh9^k*Z8$V

# Logging - Debug mode for development
logging.level.org.springframework.security=DEBUG
logging.level.com.anshul.RoomieRadarBackend=DEBUG

# Mail Server Configuration - Development
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=roomieradar2@gmail.com
spring.mail.password=noqz izmu fvvw cehv
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# File Upload Configuration - Development
file.upload-dir=./uploads
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# CORS Configuration - Development (allow local frontend)
cors.allowed-origins=http://localhost:3000,http://localhost:5173,http://localhost:5174

# Actuator Configuration - Development
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always

# Server Configuration
server.port=8080
